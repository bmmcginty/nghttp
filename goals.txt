$ Add managed spec server fixtures
$ Require Crystal >= 1.20 and clear deprecation warnings
$ Extract HTTP/1 wire protocol helpers
$ Add a generic protocol interface
$ Store selected protocol on transports
$ Move HTTP/1 request and response handling into the protocol implementation
$ Gate HTTP/1-specific handlers by protocol capabilities
$ Add ALPN protocol selection scaffolding
$ Gate the default keep-alive header by protocol
$ Allow configured protocol selection before connection setup
$ Include protocol in connection pool keys
$ Choose whether HTTP/2 uses an existing Crystal/nghttp2 dependency or an in-repo implementation
$ Add an HTTP/2-capable spec fixture
$ Add an HTTP/2 protocol implementation with frames, HPACK, stream state, and flow control
$ Register HTTP/2 in protocol selection and ALPN once the implementation exists
$ Make response body IO lifecycle work for multiplexed HTTP/2 streams
$ Audit proxy behavior for HTTP/2, especially CONNECT and TLS-over-proxy negotiation
Final desired state: HTTP/2 works over TLS ALPN and h2c for common request and response flows with safe fallback to HTTP/1
$ Patch local HPACK Huffman decoding so valid padded strings can span bytes
$ Keep HPACK Huffman workaround isolated for an upstream http2 shard PR
$ Patch local HTTP/2 HEADERS END_STREAM handling so empty bodies close correctly
$ Advertise HTTP/1.1 fallback alongside h2 for TLS ALPN
$ Cover HTTPS ALPN fallback to HTTP/1.1 when HTTP/2 is configured
$ Reapply protocol-aware request headers after ALPN changes the selected protocol
$ Serialize empty HTTP/1.1 origin-form paths as /
$ Re-enable HTTP/2 client HPACK Huffman encoding after local decoder compatibility patch
$ Add HTTP/2 POST and request body coverage
$ Add HTTPS ALPN HTTP/2 coverage
$ Make HTTP2Protocol construction safe per transport and hard to reuse incorrectly
$ Add HTTP/2 redirect, cookie, custom header, and auth coverage
$ Fix or isolate HTTP/2 Set-Cookie response handling that currently hangs with the selected shard
$ Disable shard HPACK Huffman client encoding so valid HTTP/2 paths do not trigger COMPRESSION_ERROR
$ Convert HTTP/2 receive-loop errors into request failures instead of hangs
$ Expose protocol multiplexing capability before adding stream leases
$ Make transport release idempotent so stream leases can return connections early
$ Release HTTP/2 transports after response headers so concurrent streams can share a connection
$ Map HTTP/2 GOAWAY and RST_STREAM frames to distinct library errors
$ Expose HTTP/2 RST_STREAM error codes from the shard frame reader
$ Define and implement stream leasing for true HTTP/2 multiplexing
$ Map HTTP/2 GOAWAY, RST_STREAM, and refused stream failures into library errors
$ Honor HTTP/2 SETTINGS_MAX_CONCURRENT_STREAMS when leasing streams
$ Add HTTP/2 trailer handling and coverage
$ Add HTTP/2 flow-control and large-body stress coverage
$ Handle graceful HTTP/2 GOAWAY drain for new requests on reused connections
$ Add broader HTTP/2 interoperability specs against an external server
$ Resend rewindable request bodies unchanged after retryable transport errors
$ Cover HTTPS fallback to HTTP/1.1 when ALPN negotiates no protocol
$ Document local http2-fork branches for upstream PR preparation
